# System level

use the simplest thing that can possibly work.

A class should not know all global context, and ensure that we have a global context to resolve dependencies.

Separate runtime concerns from usage